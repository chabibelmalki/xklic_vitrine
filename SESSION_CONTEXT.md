# SESSION_CONTEXT — historique (obsolète)

> ⚠️ Ce fichier était le **prompt de build initial** de la vitrine (juin 2026). Il
> décrit un plan qui **ne correspond plus** à l'architecture actuelle — notamment un
> flux `POST /api/lead → N8N_WEBHOOK_URL` et « Stripe plus tard », **abandonnés**.
>
> Le fonctionnement réel aujourd'hui (tunnel formulaire → Stripe → double écriture
> Google Sheets + Baserow) est documenté dans **`CLAUDE.md`**.
>
> Contenu d'origine conservé dans l'historique git si besoin.
